// import { Id } from "@/types";
// import { useState } from "react";

const useScrolledInView = () => {
  // const [activeSectId, setActiveSectId] = useState<Id>("");

  return (
    <div>
      <h1>useScrolledInView</h1>
    </div>
  );
};

export default useScrolledInView;
